# OpenPoke Configuration with Ananas AI
# Copy this file to .env and fill in your API keys

# Required: Ananas API key for LLM access
# Get your key from https://anannas.ai/
ANANNAS_API_KEY=your_anannas_api_key_here

# Required: Composio API key and Gmail auth config ID
# Get these from https://composio.dev/
COMPOSIO_API_KEY=your_composio_api_key_here
COMPOSIO_GMAIL_AUTH_CONFIG_ID=your_gmail_auth_config_id_here

# LLM Model Configuration - Control which model is used for each task
# Available model formats: grok-4-1-fast-reasoning-latest, openai/gpt-4o, openai/gpt-3.5-turbo, etc.
# See https://anannas.ai/models for all available models

# Interaction Agent - Handles user conversations and interprets user intent
INTERACTION_AGENT_MODEL=grok-4-1-fast-reasoning-latest

# Execution Agent - Executes tasks and uses tools
EXECUTION_AGENT_MODEL=grok-4-1-fast-reasoning-latest

# Execution Agent Search - Specifically for email search operations
EXECUTION_AGENT_SEARCH_MODEL=grok-4-1-fast-reasoning-latest

# Summarizer - Summarizes long conversations
SUMMARIZER_MODEL=grok-4-1-fast-reasoning-latest

# Email Classifier - Classifies email importance
EMAIL_CLASSIFIER_MODEL=grok-4-1-fast-reasoning-latest

# Required: YCloud WhatsApp API configuration
# Get these from https://www.ycloud.com/
YCLOUD_API_KEY=your_ycloud_api_key_here
YCLOUD_PHONE_NUMBER=+1234567890
# IMPORTANT: Get the webhook secret from YCloud API, not from the dashboard
# Run: python server/get_ycloud_secret.py to retrieve the correct secret
# The secret will include the "whsec_" prefix - keep it as-is
YCLOUD_WEBHOOK_SECRET=your_webhook_secret_here

# Optional: Skip webhook signature verification (for debugging only - NOT for production)
# YCLOUD_SKIP_SIGNATURE_VERIFICATION=true

# Optional: Server configuration
# OPENPOKE_HOST=0.0.0.0
# OPENPOKE_PORT=8001

# Optional: CORS configuration
# OPENPOKE_CORS_ALLOW_ORIGINS=*

# Optional: Documentation
# OPENPOKE_ENABLE_DOCS=1
# OPENPOKE_DOCS_URL=/docs
