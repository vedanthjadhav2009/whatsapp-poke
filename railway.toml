[build]
builder = "nixpacks"

[deploy]
startCommand = "python -m server.server"
healthcheckPath = "/api/v1/whatsapp/health"
healthcheckTimeout = 30
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
